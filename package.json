{
  "name": "linguaside",
  "version": "1.0.0",
  "description": "Learn languages while reading — powered by your own LLM API key.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "package": "node scripts/package-firefox.js",
    "lint:ext": "web-ext lint --source-dir=."
  },
  "dependencies": {
    "@preact/signals": "^1.3.0",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5",
    "web-ext": "^10.4.0"
  }
}
